
{% block title %}CommandeT{% endblock %}

{% block body %}
    <h1>CommandeT</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_t.id }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ commande_t.dateCreation ? commande_t.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Address_destination</th>
                <td>{{ commande_t.addressDestination }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_t_index') }}">back to list</a>

    <a href="{{ path('commande_t_edit', {'id': commande_t.id}) }}">edit</a>

    {{ include('commande_t/_delete_form.html.twig') }}
{% endblock %}
