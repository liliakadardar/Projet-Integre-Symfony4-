<form method="post" action="{{ path('categorie_e_delete', {'id': categorie_e.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_e.id) }}">
    <button class="btn">Delete</button>
</form>
