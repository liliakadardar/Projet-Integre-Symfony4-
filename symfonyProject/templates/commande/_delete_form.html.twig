{#  {% extends'base.html.twig' %}
{% block body %}

<form method="post" action="{{ path('delete_c', {'id': commande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.id) }}">
    <button class="btn">Delete</button>
</form>
{% endblock %}#}
