{% block title %}CategorieM{% endblock %}
{% block body %}

<form method="post" action="{{ path('categorie_m_delete', {'id': categorie_m.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_m.id) }}">
    <button class="btn">Delete</button>
</form>
{% endblock %}